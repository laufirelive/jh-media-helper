# jh-media-helper 功能里程碑规格文档

**版本**: v1.0  
**日期**: 2026-04-08  
**技术架构迁移**: 从 [ffmpeg-video-helper](file:///Users/liujiahao/ffmpeg-video-helper) (Java CLI) 迁移到 [birefnet-gui](file:///Users/liujiahao/birefnet-gui) (Python + PyQt6) 技术架构

---

## 参考项目路径

| 项目 | 路径 | 说明 |
|------|------|------|
| 源项目 (Java CLI) | `/Users/liujiahao/ffmpeg-video-helper` | 现有功能实现，命令行交互 |
| 目标架构参考 | `/Users/liujiahao/birefnet-gui` | PyQt6 GUI 架构、队列系统、配置管理 |

---

## 架构迁移说明

### 从 ffmpeg-video-helper 继承
- 核心 FFmpeg 处理逻辑（命令构建、流分析）
- 功能业务逻辑（图片序列编码、音视频混合、MKV解包）
- 参数默认值和校验规则

### 从 birefnet-gui 借鉴
- PyQt6 GUI 框架和组件模式
- 任务队列系统（QueueManager + QueueTask）
- 配置持久化（dataclass + JSON）
- Worker 线程模式（QThread + 信号槽）
- 分阶段进度报告（Pipeline phases）
- 界面布局（Tab 结构：单任务 / 批量队列 / 设置）

---

## 里程碑总览

| 里程碑 | 功能 | 优先级 | 状态 |
|--------|------|--------|------|
| M1 | PictureSeqConvertToMp4（图片序列转视频） | P0 | 核心功能 |
| M2 | CombatVideoWithAudios（音视频混合） | P0 | 核心功能 |
| M3 | VideoExtractor（MKV解包） | P0 | 核心功能 |
| M4 | 队列持久化系统 | P0 | 基础设施 |

---

## M1: PictureSeqConvertToMp4（图片序列转视频）

### 功能描述
将图片序列编码为视频文件，支持硬件加速。

### 输入参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 图片序列文件夹 | 路径 | - | 单文件夹选择 |
| 帧率 | 整数 | 120 | fps |
| 比特率 | 整数 | 32 | 单位 Mbps |
| 硬件加速 | 布尔 | True | hevc_nvenc，失败自动回退 libx264 |
| 分辨率宽 | 整数 | 3840 | - |
| 分辨率高 | 整数 | 2160 | - |
| 扫描格式 | 字符串 | %06d.png | 文件名匹配模式 |
| 输出目录 | 路径 | 原文件夹同级 | 可选自定义 |

### 输出规格
- **文件名**: `s{文件夹名}.mp4`
- **格式**: MP4 (H.264/H.265)
- **位置**: 指定输出目录或原文件夹同级目录

### 关键行为
1. 扫描文件夹，按扫描格式匹配图片文件
2. 显示检测到的图片数量
3. 硬件加速失败时自动回退到 libx264
4. 编码完成后显示输出路径

### 错误处理
- 文件夹不存在或无匹配图片：前置校验报错
- FFmpeg 编码失败：显示错误日志

---

## M2: CombatVideoWithAudios（音视频混合）

### 功能描述
批量为视频/音频混合背景音乐，支持并行处理。

### 输入参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 并行线程数 | 整数 | 1 | 并发处理数 |
| 音频目录 | 路径 | - | 背景音乐库文件夹 |
| 原视频响度 | 浮点 | 0.6 | 0-1 范围 |
| 是否封装 | 布尔 | False | True=输出MKV，False=仅输出音频 |
| 视频文件列表 | 路径列表 | - | 支持视频或纯音频文件 |

### 处理逻辑

**输入类型判断**：
- 纯音频文件（aac/mp3/wav）→ 直接作为基底音频
- 视频文件 → 提取音频流作为基底音频

**音频时长对齐**：
- 背景音乐时长 < 基底时长：循环拉长
- 背景音乐时长 > 基底时长：裁剪

**混合公式**：`输出 = 基底音频 × 原视频响度 + 背景音乐 × 1.0`

### 输出规格

**封装=False**：
```
{视频名}_mixed_0.aac  (混合音频1)
{视频名}_mixed_1.aac  (混合音频2)
...
```

**封装=True**：
```
{视频名}_{timestamp}.mkv  (视频+混合音频1)
{视频名}_{timestamp}.mkv  (视频+混合音频2)
...
```

### 关键行为
1. 扫描音频目录，列出所有音频文件
2. 并行处理多个背景音乐（受线程数限制）
3. 临时文件自动清理
4. 显示处理进度（当前视频/总视频）

### 错误处理
- 音频目录不存在：前置校验报错
- 视频无音频流：跳过并提示
- 混合失败：记录错误继续下一项

---

## M3: VideoExtractor（MKV解包）

### 功能描述
从 MKV 文件中提取指定视频流和音频流，无损转封装为 MP4。

### 输入参数

| 参数 | 类型 | 说明 |
|------|------|------|
| MKV 文件 | 路径 | - |
| 流配对列表 | 结构体列表 | 每个元素={视频流索引, 音频流索引} |

### 流选择界面
1. 使用 ffprobe 分析 MKV 中的所有流
2. 显示视频流列表（索引、编码、分辨率、时长）
3. 显示音频流列表（索引、编码、语言、时长）
4. 用户选择视频流+音频流配对（支持多选）

### 输出规格
- **文件名**: `{原文件名}_{序号}.mp4`
- **格式**: MP4 (copy 模式，不重新编码)
- **位置**: 原文件同级目录

### 关键行为
1. 仅支持 H.264/HEVC 编码的视频流直接 copy
2. 不兼容编码提前报错，不自动转码
3. 音频流可选（null 表示无音频）
4. 多配对批量输出

### 错误处理
- 非 MKV 文件：前置校验报错
- 视频编码不兼容：提示并跳过该流
- 提取失败：显示 FFmpeg 错误信息

---

## M4: 队列持久化系统

### 功能描述
任务队列的保存、恢复和管理。


### 持久化文件
- **路径**: `~/.ffmpeg-video-helper/queue.json`
- **格式**: JSON

### 恢复策略
1. 启动时读取 queue.json
2. 状态为 processing 的任务自动改为 paused
3. 弹窗询问用户：继续 / 清空 / 忽略

### 队列操作
- 添加任务
- 删除任务
- 调整顺序（上移/下移）
- 清空队列
- 开始/暂停/继续处理

---

## 界面布局规格（参照 birefnet-gui）

### Tab 结构
```
┌─────────────────────────────────────────┐
│ [单任务] [批量队列 (N)] [设置]           │
└─────────────────────────────────────────┘
```

### 单任务 Tab
```
┌─────────────────┬───────────────────────┐
│                 │                       │
│  输入文件选择   │    参数配置面板        │
│  [浏览...]      │    （根据任务类型动态） │
│                 │                       │
│  文件信息       ├───────────────────────┤
│  分辨率|时长    │    预设/历史配置       │
│                 │                       │
│  预览区（可选）  │                       │
│                 │                       │
├─────────────────┴───────────────────────┤
│ [预览] [开始处理] [暂停] [取消] [加入队列] │
└─────────────────────────────────────────┘
```

### 任务类型选择器
- 下拉菜单或图标按钮
- 选项：图片序列转视频 / 音视频混合 / MKV解包

### 批量队列 Tab
- 任务列表（可拖拽排序）
- 显示：任务类型、输入文件、状态、进度
- 操作：开始队列、暂停、清空、删除选中

---

## 技术约束

| 项目 | 规格 |
|------|------|
| Python | 3.10+ |
| GUI | PyQt6 |
| FFmpeg | 命令行调用 |
| 配置存储 | JSON 文件 |
| 队列持久化 | JSON 文件 |

---

## 验收标准

### M1 验收
- [ ] 选择图片文件夹，设置参数，成功生成 MP4
- [ ] 硬件加速失败时自动回退软件编码
- [ ] 任务可加入队列，队列可持久化恢复

### M2 验收
- [ ] 选择视频+音频目录，成功生成混合音频/MKV
- [ ] 并行线程数生效
- [ ] 临时文件自动清理

### M3 验收
- [ ] 选择 MKV 文件，显示流信息
- [ ] 选择流配对，成功提取 MP4
- [ ] 不兼容编码正确报错

### M4 验收
- [ ] 队列关闭后重新打开，任务状态正确恢复
- [ ] 支持继续/清空/忽略三种恢复策略

---

## 相关文件路径汇总

| 类型 | 路径 |
|------|------|
| 本规格文档 | `/Users/liujiahao/jh-media-helper/SPEC.md` |
| 源项目 (Java) | `/Users/liujiahao/ffmpeg-video-helper` |
| 架构参考 (Python) | `/Users/liujiahao/birefnet-gui` |
| 目标项目 | `/Users/liujiahao/jh-media-helper` |

---

*文档完成，可交付开发。*
